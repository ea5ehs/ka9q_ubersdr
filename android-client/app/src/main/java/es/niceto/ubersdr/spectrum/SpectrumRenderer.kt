package es.niceto.ubersdr.spectrum

import es.niceto.ubersdr.model.SpectrumConfig
import es.niceto.ubersdr.model.SpectrumFrame

class SpectrumRenderer {
    fun updateConfig(config: SpectrumConfig) {
        // TODO: Store current spectrum config for waterfall rendering
    }

    fun render(frame: SpectrumFrame) {
        // TODO: Render incoming SPEC frame into mutable bitmap
    }
}
